# Reference docs — source & attribution

The `docs_*` and `*-examples.json` files in this folder are **mirrored verbatim** from the upstream project:

- **Project:** ThreeHats / `foundryvtt-rest-api-relay`
- **Repo:** https://github.com/ThreeHats/foundryvtt-rest-api-relay (branch `main`)
- **License:** MIT — © the upstream authors. These mirrored files remain under their original MIT license.
- **Why mirrored:** offline ground-truth for implementing this MCP client, so the spec doesn't drift mid-build.

Regenerate at any time with [`../scripts/fetch-reference.sh`](../scripts/fetch-reference.sh).

This repo's own code and docs (everything outside `reference/`) are licensed separately — see the top-level [`LICENSE`](../LICENSE).

> Build-time deliverable that also lives here: `dnd5e-5.2.2-npc-schema.md` (produced by the Phase 4 schema spike) — that file is original work for this project, not upstream content.
