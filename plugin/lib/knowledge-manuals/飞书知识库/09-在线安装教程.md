# Docs

> 来源: https://xcnplziulnma.feishu.cn/wiki/U0jPwSbgmiYmCoktBbPcJD19nIf

[手动安装教程](https://xcnplziulnma.feishu.cn/wiki/TatpwFDdQiAEzekbIuFcfOTDnUh)[FVTT 下载加速代理 v12-14支持](https://xcnplziulnma.feishu.cn/wiki/GBLNw30Biiq6iKk8DF8cx4oMnAb)如果你的服务器无法直连Github，你需要首先安装 。但是需要注意，对于一些挂载在外网，如Google云盘上的mod来说，上述的两个方法无能为力，你需要考虑使用 。
以mod安装为例，系统的安装是类似的，你首先需要切回FVTT的主界面，并切换到模组插件子标签页，并点击右上角的安装模组
![图片](images/XfOSbfq95oGxBUxRJUKcr0EHnxe.png)
如果你没有这个按钮，那你的当务之急应该是重装你的fvtt而不是到群里折磨别人
> 💡 
虽然我们演示的截图是在客户端上，但是你很明显可以在本地打开网页访问你的fvtt来进行同样的操作
之后会弹出一个界面，我们称之为模组安装器
![图片](images/CFnrbPSipoVsBQxgD7Pc1DFenzd.png)
在这里，我们有两种方法来安装mod
# 直接安装
当你很确信你的系统与核心都支持你直接使用最新的mod版本时，你可以在模组安装器内直接搜索对应的mod名，并点击安装，同时系统也会帮助你安装对应的依赖项mod（如果有）
![图片](images/KNGtbqleBo6g1TxloEvcbbP7n4b.png)
# 使用清单地址
有时你需要一些稍旧版本/特定版本的mod，或是你要安装的mod虽然仓库位于GitHub，但并没有上架官方的mod商店，你会需要使用清单地址来安装mod，同样的，以这种形式安装的mod系统也会帮助你安装对应的依赖项mod（如果有）。
首先，你需要找到对应的清单地址，我们有以下几种方法：
## 官网
你需要前往官网的mod网址，比如https://foundryvtt.com/packages/midi-qol，然后下拉，到最下面的Avalible Versions，你会看到一系列版本号，以及他们支持的核心版本，Mainifest URL与Read Notes（更新日志）。找到你要安装的版本
![图片](images/X0MwbX8W7o2VfvxBhg8cBIcdnrh.png)
以此图为例，我们要下载v13最新的版本，也就是v13.0.64，右键对应的Mainifest URL并选择复制链接
![图片](images/LMWRbuoOPoNwlhxgvvjclNocnCc.png)
之后将复制的链接粘贴到模组管理器下方的输入框内，点击安装即可
![图片](images/BtvNbngsioxSdhxGZQBcQiwMnnc.png)
## GitHub/GitLab
有的mod作者并不会辛勤维护官方的mod页面，因此如果你要下载特定版本，需要前往他们的仓库，你可以直接在官方的mod页面点击project里的链接进入他们的仓库
![图片](images/IE63bXTuWo5SbTxxclPc3SX2nYd.png)
之后找到对应的release页面
![图片](images/ADombQYaforCLJxH9UBcdMTwnfd.png)
> 💡 
部分作者也会在readme开头直接放上可供粘贴的清单地址，很明显，我们这个例子并没有
在release页面找到你要安装的版本
![图片](images/A7ZsbjNXnoiWDWxu1pmcbVWUnjh.png)
右键Assets里的module.json，复制链接
![图片](images/Md6ObB4SGovp3yxpRI7cBrg0nlh.png)
将其粘贴进入模组管理器下方的输入框内，点击安装即可
![图片](images/GuMjb0IkJo1QNWxfRMNcFVgvnXc.png)
# 通过脚本批量安装
你可以在主界面打开控制台，输入以下脚本来批量安装mod，但需要你提供对应的清单地址，同样地，系统也会自动弹出对话框询问你是否安装依赖项：
```
(async () => {
  const manifests = [
    "https://example.com/module-a/module.json",   //替换为你要安装的mod的清单地址
    "https://example.com/module-b/module.json",
    "https://example.com/module-c/module.json"
  ];

  const results = [];

  for (const manifest of manifests) {
    try {
      console.log(`正在安装：${manifest}`);

      const pkg = await game.installPackage({
        manifest,
        notify: true
      });

      results.push({
        id: pkg.id,
        title: pkg.title,
        status: "installed"
      });
    } catch (error) {
      console.error(`安装失败：${manifest}`, error);
      results.push({
        manifest,
        status: "failed",
        error: error.message
      });
    }
  }

  console.table(results);
})();
```
如果你只有mod的id，也可以使用脚本来批量安装，前提是他们已经上架官方的mod列表
```
(async () => {
  const ids = [
    "ID1",               //替换为你要安装的mod ID
    "ID2",
    "ID3"
  ];

  const catalog = await game.getPackages({type: "module"});
  const results = [];

  for (const id of ids) {
    if (game.modules.has(id)) {
      console.log(`已安装，跳过：${id}`);
      results.push({id, status: "skipped"});
      continue;
    }

    const info = catalog.get(id);

    if (!info) {
      console.error(`官方目录中找不到：${id}`);
      results.push({id, status: "not-found"});
      continue;
    }

    try {
      const pkg = await game.installPackage({
        id,
        manifest: info.manifest,
        notify: true
      });

      results.push({
        id: pkg.id,
        title: pkg.title,
        status: "installed"
      });
    } catch (error) {
      console.error(`安装失败：${id}`, error);
      results.push({id, status: "failed", error: error.message});
    }
  }

  console.table(results);
})();
```
每次都需要更改脚本中的ids或是manifests可能会稍显庞杂，你也可以在本地维护一个特殊的json，然后读取json里的ids/manifests来进行批量安装，此时脚本如下
```
(async () => {
  // 打开本地 JSON 文件
  const file = await new Promise(resolve => {
    const input = document.createElement("input");
    input.type = "file";
    input.accept = ".json,application/json";
    input.onchange = () => resolve(input.files?.[0]);
    input.click();
  });

  if (!file) {
    console.warn("没有选择文件");
    return;
  }

  let json;
  try {
    json = JSON.parse(await file.text());
  } catch (error) {
    console.error("JSON 格式错误：", error);
    ui.notifications.error(`JSON 格式错误：${error.message}`);
    return;
  }

  // 同时支持 [...]
  // 以及 { "packages": [...] }、{ "modules": [...] }
  const entries = Array.isArray(json)
    ? json
    : json.packages ?? json.modules;

  if (!Array.isArray(entries)) {
    throw new Error(
      'JSON 顶层必须是数组，或包含 "packages"/"modules" 数组'
    );
  }

  // 标准化条目
  const packages = entries.map(entry => {
    if (typeof entry === "string") {
      return /^https?:\/\//i.test(entry)
        ? {type: "module", manifest: entry}
        : {type: "module", id: entry};
    }

    return {
      type: entry.type ?? "module",
      id: entry.id,
      manifest: entry.manifest
    };
  });

  const collections = {
    module: game.modules,
    system: game.systems,
    world: game.worlds
  };

  // 只在存在纯 ID 条目时读取对应官方目录
  const catalogs = {};

  for (const type of new Set(
    packages.filter(p => p.id && !p.manifest).map(p => p.type)
  )) {
    console.log(`正在读取 ${type} 官方目录……`);
    catalogs[type] = await game.getPackages({type});
  }

  const results = [];

  for (const entry of packages) {
    const {type, id} = entry;
    let {manifest} = entry;

    if (!["module", "system", "world"].includes(type)) {
      console.error(`不支持的包类型：${type}`);
      results.push({type, id, manifest, status: "invalid-type"});
      continue;
    }

    // 已安装则跳过
    if (id && collections[type]?.has(id)) {
      console.log(`已安装，跳过：${type}/${id}`);
      results.push({type, id, status: "skipped"});
      continue;
    }

    // 根据 ID 从官方目录取得 manifest
    if (!manifest && id) {
      const catalogEntry = catalogs[type]?.get(id);

      if (!catalogEntry) {
        console.error(`官方目录中找不到：${type}/${id}`);
        results.push({type, id, status: "not-found"});
        continue;
      }

      manifest = catalogEntry.manifest;

      if (!manifest) {
        console.error(`包没有可用 manifest：${type}/${id}`);
        results.push({type, id, status: "no-manifest"});
        continue;
      }
    }

    if (!manifest) {
      console.error("条目缺少 id 或 manifest：", entry);
      results.push({...entry, status: "invalid-entry"});
      continue;
    }

    try {
      console.log(`正在安装：${type}/${id ?? manifest}`);

      const installed = await game.installPackage({
        type,
        id,
        manifest,
        notify: true,
        force: false
      });

      results.push({
        type,
        id: installed.id,
        title: installed.title,
        status: "installed"
      });
    } catch (error) {
      console.error(`安装失败：${id ?? manifest}`, error);
      results.push({
        type,
        id,
        manifest,
        status: "failed",
        error: error.message
      });
    }
  }

  console.table(results);

  // 保留结果，之后可在控制台查看
  window.fvttBatchInstallResults = results;

  const installed = results.filter(r => r.status === "installed").length;
  const skipped = results.filter(r => r.status === "skipped").length;
  const failed = results.length - installed - skipped;

  ui.notifications.info(
    `批量处理完成：安装 ${installed}，跳过 ${skipped}，其他 ${failed}`
  );

  return results;
})();
```
然后在本地创建一个json，命名无所谓，你可以参考以下示例
📎 文件: 
运行脚本后在弹出的文件选择器选择你创建的json即可。